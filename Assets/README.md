# Assets — бэкенд (Spring Boot)

API для KEYED Vault. Фронт: `../Assets-Frontend/keyed-vault-frontend`.

## Запуск

```powershell
docker compose up -d
pip install -r req.txt
.\mvnw.cmd spring-boot:run
```

Порт: **8080**. Полная инструкция с фронтом: [../README.md](../README.md).
